version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: coffee_shop_db
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-coffee_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-coffee_pass_2026}
      POSTGRES_DB: ${POSTGRES_DB:-coffee_shop}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-coffee_user}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
